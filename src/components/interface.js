import React from "react";

export default function Interface() {return (
    <main id="interfaceContainer">
        <header>
            <button></button>
            <div></div>
        </header>

    </main>

    // Interface Design: CRUD actions to content, view and delete notes from dashboard
        // Project Full: create new project, update existing project details and to-do list, delete project/to-do's
        // Project + Series: 
        // Series Full: CRUD plot categories, plotlines, groups, subgroups, points; CRUD books/trilogies; edit series details
        // Series + Book:
        // Book Full: assign/unassign plotpoints, CRUD assigned plotpoints, CRUD acts/chapters, update book details
        // Book + Chapter: 
        // Chapter Full: assign/unassign plotpoints, CRUD assigned plotpoints, CRUD acts/scenes/moments, update chapter details
        // Chapter + Draft: 
        // Draft Full: CRUD paragraphs, CRUD sentences, view and delete reader notes, assign/unassign plotpoints to sentences
        // 
        // 
        // 

)}